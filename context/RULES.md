# RULES — Project Coding Rules

> **Purpose:** The project's own coding rules, grounded in the clean-code standard in `core/decision-framework.md` §3 and adapted to this project's actual language and conventions. Tier-3 template — fill it in. These rules sit at tier 3 and override the generic standard where they legitimately diverge for this project.

_Last updated: [DATE]_

## Language & Conventions

[PLACEHOLDER: Primary language(s), version, formatter/linter, and the style guide the project follows.]

## Guiding Principles

Apply these as adapted to this project's language:

- **Clean-code baseline** — functions, naming, comments, and error handling per `core/decision-framework.md` §3. Do not restate it here; record only project-specific deviations or specifics.
- **SOLID** — [PLACEHOLDER: how SOLID applies here, or which parts matter most for this codebase.]
- **DRY** — extract a shared abstraction only once a pattern genuinely repeats (not on the first occurrence). [PLACEHOLDER: project-specific note.]
- **KISS** — prefer the simplest design that meets the requirement. [PLACEHOLDER: project-specific note.]

## Project-Specific Rules

[PLACEHOLDER: Conventions unique to this project — folder structure, module boundaries, naming schemes, state management, API/versioning rules, logging format.]

## Testing Convention

[PLACEHOLDER: Test framework, where tests live, coverage expectations, and whether new code is _expected_ to ship with tests. This is what tells the agent whether to add tests by default — see `core/decision-framework.md` §6.]

## Commit & PR Conventions

[PLACEHOLDER: Commit message format, branch naming, PR description expectations, and required checks.]
